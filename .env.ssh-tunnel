#!/bin/bash
# SSH隧道配置，在创建隧道时自动加载

# 本地服务端口
LOCAL_PORT=8000

# 公网服务器配置
REMOTE_HOST=lyh.ai
REMOTE_PORT=12345
REMOTE_USER=user  # 需要替换为实际用户名

# SSH选项
SSH_OPTIONS="-o ServerAliveInterval=60 -o ExitOnForwardFailure=yes"

# 日志文件
LOG_FILE=ssh-tunnel.log 